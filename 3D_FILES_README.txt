3D FILES INCLUDED

Primary website model:
  assets/hyrax.glb

Also included as an unpacked glTF representation:
  assets/extracted_gltf/hyrax.gltf
  assets/extracted_gltf/hyrax.bin

The existing index.html already loads ./assets/hyrax.glb using WebGL2.
Do not delete hyrax.glb if you want the current site to render the model.
